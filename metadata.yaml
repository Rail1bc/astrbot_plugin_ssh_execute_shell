name: astrbot_plugin_ssh_execute_shell # 插件唯一标识名，以 astrbot_plugin_ 前缀开头
display_name: SSH命令执行器 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: AstrBot SSH命令执行插件，支持通过SSH协议远程执行Shell命令。 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: Rail1bc, 寒露 # 作者
repo: https://github.com/Rail1bc/astrbot_plugin_ssh_execute_shell # 插件的仓库地址
